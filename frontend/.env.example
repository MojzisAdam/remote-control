# -------------------------------
# API Configuration
# -------------------------------

# Base API endpoint used by frontend to communicate with the backend (Laravel)
VITE_API_URL=http://localhost:8000/api

# Base URL for the backend
VITE_API_URL_BASE=http://localhost:8000/

# -------------------------------
# Cookies & Domains
# -------------------------------

# The domain where cookies will be stored (e.g., localhost, yourdomain.com)
VITE_COOKIE_DOMAIN=localhost

# -------------------------------
# MQTT / WebSocket Configuration
# -------------------------------

# MQTT host URL for real-time communication
# Use `wss://` for secure WebSocket (SSL), or `ws://` for local testing without SSL
VITE_MQTT_HOST=wss://localhost

# MQTT port number
VITE_MQTT_PORT=8883

# Optional: MQTT authentication (if your broker requires credentials)
VITE_MQTT_USERNAME=
VITE_MQTT_PASSWORD=